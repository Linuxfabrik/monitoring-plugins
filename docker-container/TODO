Sollte ausgeben, welche Container es gefunden hat.

docker run hello-world
=> lädt einen Container runter

=> gibt WARN

müsste Ergebnis von `docker container ls --all` ausgeben: "No containers running, but found ..."