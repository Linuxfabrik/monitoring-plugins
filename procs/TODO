Output:

	220 Tasks (574 thr), 1 run, 158 slp, 61 oth



ps -e | grep -E '( R| D| Z)'

R    Marks a runnable process.
D    Marks an uninterruptible process, because it is in disk wait.
Z    Marks a Zombie process; reboot required. WARN bei >= 1

Inkl. Aufzählung, welche RDZ gefunden wurden.




# icinga processes plugin
processes with states Z,R,D
https://psutil.readthedocs.io/en/latest/#process-status-constants
https://psutil.readthedocs.io/en/latest/#psutil.Process.status

* count procs per user and compare value against `ulimit -u` (default: 80%, 90%)