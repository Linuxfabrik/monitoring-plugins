# On RHEL8:
#   sudo dnf -y install python39-pip
#   python3.9 -m pip install --user --upgrade pip
#   python3.9 -m pip install --user pip-tools
#   python3.9 -m piptools compile --generate-hashes requirements.in
#
# If you get `AttributeError: 'function' object has no attribute 'cache_clear':
#   python3.9 -m pip install 'pip<25.1'

beautifulsoup4
flatdict
linuxfabrik-lib
lxml --only-binary=lxml
psutil
python-keystoneclient
python-novaclient
python-swiftclient
pyyaml
smbprotocol
vici
xmltodict
