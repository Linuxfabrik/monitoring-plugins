# What the RHEL and the Debian family ship: the drop-in directory first, so a
# file in it wins over everything below.
Include /etc/ssh/sshd_config.d/*.conf

LogLevel ERROR
PermitRootLogin no

Match Address 198.51.100.0/24
    LogLevel QUIET
