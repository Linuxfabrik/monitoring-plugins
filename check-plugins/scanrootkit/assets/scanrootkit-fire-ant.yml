# Source: https://www.sygnia.co/blog/fire-ant-evolves-from-hypervisors-to-trusted-infrastructure/
# Published: 2026 Sygnia (2026-08-27), Linux management hosts compromised by Fire Ant (UNC3886 cluster)
# The Medusa files under /usr/lib/locate from the same report live in
# scanrootkit-medusa-ldpreload.yml. Left out: /bin/atd, /bin/gdm and
# /usr/bin/acpid (resolve to legitimate binaries on merged-/usr hosts), the
# impersonated Cybereason and SentinelOne agent paths, the IOS XR router paths,
# and the generic /var/tmp names (.bashrc, audit, ping, sync, ttt.tar, u6.py)
# and /var/run/acpid.lock.
name: 'Fire Ant TacTap, BridgeAgent and SSH backdoors'
files:
  - '/etc/rc.d/init.d/grub-rommon'
  - '/lib/libseconfd.so'
  - '/opt/.ICEauthority'
  - '/usr/sbin/acppid'
  - '/usr/sbin/cupsdd'
  - '/usr/sbin/smartdd'
  - '/var/log/.tacplus.acct'
  - '/var/tmp/esv3X'
  - '/var/tmp/hourglass-cn'
  - '/var/tmp/tacacs.pcap'
dirs: []
ksyms: []
discovered: '2025'
cl: 100
