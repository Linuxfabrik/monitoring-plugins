# The package manager merges every versionlock.conf it finds into one configuration, and
# the directory below overrides the `locklist` set here. This list is therefore never
# read and this lock is not in force.
must-not-be-read-0:1.0-1.el9.*
