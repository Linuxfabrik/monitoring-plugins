# Added lock on Mon Aug 10 11:57:28 2026
# The spelling the package manager writes itself.
bash-0:4.4.20-6.el8_10.*
# The same lock with the epoch in front, which the package manager cannot read at all:
# it prints "could not parse pattern" and holds nothing.
0:glibc-2.28-251.el8_10.*
# Package names whose second-to-last dash-separated field starts with a digit. Both are
# names and neither carries a version, so neither may be cut in half.
java-1.8.0-openjdk
java-17-openjdk
# A version and release without an epoch, which is still a version to lock to.
python3-foo-1.2-3.el9
