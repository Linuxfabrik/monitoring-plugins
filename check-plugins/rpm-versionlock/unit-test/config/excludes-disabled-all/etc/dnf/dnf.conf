[main]
gpgcheck=1
installonly_limit=3
excludepkgs=main-config-package
# `all` covers every scope at once. Verified against dnf 4.14 on Rocky 9: the package
# excluded above installs again as soon as this line is present.
disable_excludes=all
