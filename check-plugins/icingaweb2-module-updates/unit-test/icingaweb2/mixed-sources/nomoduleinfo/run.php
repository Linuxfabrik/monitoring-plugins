<?php
// A module that ships no module.info. Icinga Web 2 still lists it and reports no
// version for it, which is what the `ok-module-without-a-module-info-is-not-outdated`
// testcase checks.
//
// This file only exists so that git tracks the directory: the fixture is the
// *absence* of module.info, and git does not store empty directories.
