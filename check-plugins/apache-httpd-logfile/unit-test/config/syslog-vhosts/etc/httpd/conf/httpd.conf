# The server hands its own log to syslog while the sites keep files of their
# own, which is what our configuration management sets up. Reading nothing at
# all because of the first directive would leave the sites unwatched.
ServerRoot "/etc/httpd"
ErrorLog syslog:local1
IncludeOptional conf.d/*.conf
