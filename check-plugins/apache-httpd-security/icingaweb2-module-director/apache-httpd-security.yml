---
variants:
  - linux
  - sudo

overwrites:
  '["ServiceTemplate"]["tpl-service-apache-httpd-security"]["check_interval"]': 86400
  '["ServiceTemplate"]["tpl-service-apache-httpd-security"]["enable_perfdata"]': true
  '["ServiceTemplate"]["tpl-service-apache-httpd-security"]["retry_interval"]': 3600
  # The Apache Service Sets ship the status check, which reads mod_status, so
  # the module is present on purpose on every host that runs both. The check
  # name as it stands, with no anchors: --ignore matches anywhere in the name,
  # and no anchor here means no "$" that Icinga's macro resolver would read as
  # an unclosed macro delimiter.
  '["ServiceTemplate"]["tpl-service-apache-httpd-security"]["vars"]["apache_httpd_security_ignore"]':
    - 'Status module'
