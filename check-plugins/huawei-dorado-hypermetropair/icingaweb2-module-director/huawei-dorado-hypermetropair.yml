variants:
  - linux

overwrites:
  '["ServiceTemplate"]["tpl-service-huawei-dorado-hypermetropair"]["check_interval"]': 300
  '["ServiceTemplate"]["tpl-service-huawei-dorado-hypermetropair"]["retry_interval"]': 30
  '["ServiceTemplate"]["tpl-service-huawei-dorado-hypermetropair"]["use_agent"]': false
  '["ServiceTemplate"]["tpl-service-huawei-dorado-hypermetropair"]["vars"]["criticality"]': 'A'
  # A service listing every healthy item alongside the faulty one is unreadable on a
  # sizeable appliance, so the service set ships with the rows within the thresholds
  # hidden. Perfdata and alerting are unaffected.
  '["ServiceTemplate"]["tpl-service-huawei-dorado-hypermetropair"]["vars"]["huawei_dorado_hypermetropair_brief"]': true
