# A preferences file that holds nothing but a comment, which is what a host looks like
# where pinning was set up once and then emptied out again. APT reads it and finds no
# stanza, so nothing is pinned and nothing is refused.
