# The stanza APT applies.
Package: valid-package
Pin: version 1.24.*
Pin-Priority: 1001

# The `Explanation` lost its colon and is the last line of the file, so the field name it
# opens never finds the colon that would close it. APT does not stop reading quietly here:
# verified against Debian 12, it refuses the file with "Unable to parse package file
# /etc/apt/preferences (1)" and every command that works out package priorities fails
# afterwards, `apt-get install` and `apt-get upgrade` included.
Explanation this line has no colon
