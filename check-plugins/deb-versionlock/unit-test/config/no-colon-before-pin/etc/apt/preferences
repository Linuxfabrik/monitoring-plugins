# The stanza APT applies.
Package: valid-package
Pin: version 1.24.*
Pin-Priority: 1001

# The `Explanation` lost its colon and swallows the `Pin` below it into its own field
# name. The stanza keeps its `Package` header, so APT does not refuse the file - it just
# finds no pin type and skips the stanza. The package is not pinned.
Package: no-pin-left-package
Explanation why we pin
Pin: version 6.0*
Pin-Priority: 1001

# The stanza behind it is applied like any other, because APT did not stumble.
Package: after-the-swallowed-stanza-package
Pin: version 7.1*
Pin-Priority: 1001
